import { MiDirectiva1Directive } from './mi-directiva1.directive';

describe('MiDirectiva1Directive', () => {
  it('should create an instance', () => {
    const directive = new MiDirectiva1Directive();
    expect(directive).toBeTruthy();
  });
});
